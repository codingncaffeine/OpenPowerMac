# Build output
build/
build-*/
out/
cmake-build-*/
.vs/
.vscode/
*.user

# Dev-shell local state
imgui.ini
CMakeUserPresets.json

# Local planning docs — never pushed
_plans/

# Apple ROMs, OS media, and disk images — user-supplied, never committed
*.rom
roms/
*.img
*.image
*.dsk
*.hfs
*.dmg
*.cdr
*.iso
*.toast
*.cue
*.bin
*.smi
*.sit
*.sea
*.hqx

# Vendor manuals and locally fetched test data — reference shelf lives in scratch, never in-repo
docs/refs/
tests/data/

# derived screen dumps
*.ppm

# WPF shell build artifacts (native/ receives opmcapi.dll from CMake)
shell/bin/
shell/obj/
shell/native/

# Release artifact (dotnet publish) — built every pass, never committed
shell/publish/
